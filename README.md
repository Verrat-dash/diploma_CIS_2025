# diploma_CIS_2025
Ansible playbooks для автоматизации тестирования безопасности CIS Benchmark для Windows и Ubuntu

# 🛡️ Автоматизация базового харденинга ОС Windows и Ubuntu на основе CIS Benchmarks

**Дипломный проект**

## 🎯 Цель проекта
Разработка прототипа системы для **автоматизированного приведения ключевых параметров безопасности** операционных систем Windows и Linux в соответствие с рекомендациями стандарта **CIS Benchmarks**. Решение позволяет быстро и воспроизводимо настраивать безопасность рабочих станций, устраняя человеческий фактор и обеспечивая консистентность конфигураций.

## 🛠 Технологический стек
*   **Ansible** — система управления конфигурацией (ядро автоматизации).
*   **Vagrant** — создание и управление детерминированными виртуальными стендами.
*   **VirtualBox** — гипервизор для виртуальных машин.
*   **WSL2 (Ubuntu)** — среда для запуска Ansible на хосте Windows.

## 📁 Структура репозитория

```text
diploma_CIS_2025/
├── Vagrantfile                     # Декларативное описание виртуальных машин
├── inventory.ini                   # Файл инвентаризации для Ansible
├── playbooks/
│   ├── apply-ubuntu-cis.yml        # Главный плейбук для Ubuntu
│   ├── apply-windows-cis.yml       # Главный плейбук для Windows
│   └── roles/                      # Роли с задачами
│       ├── ubuntu-cis/
│       │   └── tasks/
│       │       ├── 1_Initial_Setup/     # Настройки лежат в разделах
│       │       ├── 5_Access_Control/
│       │       └── ...                  # Другие разделы CIS
│       └── windows-cis/
│           └── tasks/
│               ├── 26_Device_Lock/
│               ├── 49_Local_Policies_Security_Options/
│               └── ...                   # Другие разделы CIS
├── screenshots/                     # Скриншоты выполнения
└── README.md                        # Документация проекта
```

## 📋 Реализованные настройки CIS Benchmarks
Система автоматизирует применение **43 базовых рекомендаций** (Level 1/2) для усиления защиты от атак на аутентификацию, несанкционированный доступ и вредоносное ПО.

### Для Ubuntu Linux (21 настройка)
**Примеры ключевых рекомендаций:**
*   **5.1.16** — Ограничение попыток аутентификации SSH (`MaxAuthTries 4`)
*   **5.1.20** — Запрет прямого входа `root` по SSH
*   **5.3.3.2.2** — Установка минимальной длины пароля (`minlen=14`)
*   **5.3.3.3.1** — Включение истории паролей (`remember=24`)
*   **1.7.2** — Настройка предупреждающего баннера входа в GDM
*   **4.2.1, 4.2.4, 4.2.8** — Установка и настройка фаервола UFW

### Для Microsoft Windows (22 настройки)
**Примеры ключевых рекомендаций:**
*   **26.4, 26.5, 26.8, 26.9** — Политики паролей (срок действия, история, длина)
*   **26.6, 26.7** — Блокировка учетной записи и экрана
*   **49.9** — Текст предупреждения при интерактивном входе
*   **4.11.27.1** — Блокировка личных учетных записей Microsoft
*   **38.1, 38.4, 38.7** — Включение брандмауэра для всех профилей сети
*   **22.26** — Включение сетевой защиты Microsoft Defender

## 🖼️ Скриншоты выполнения
*Скриншоты выполнения будут добавлены в соответствующие папки*

## 🚀 Инструкция по запуску тестового стенда

> **⚠️ Важное примечание:** Данный стенд предназначен для тестирования в изолированной среде (ваш ПК). Для работы потребуется скачать виртуальные образы ОС общим объёмом ~10-15 ГБ. Убедитесь, что на диске достаточно свободного места и включена виртуализация в BIOS/UEFI.

### 📦 Шаг 0: Подготовка ПО на хостовой машине
Перед началом необходимо установить базовое программное обеспечение на ваш компьютер:

1.  **Установите VirtualBox** – гипервизор для запуска виртуальных машин.
    *   Скачайте с [официального сайта](https://www.virtualbox.org/).
    *   Во время установки согласитесь на инсталляцию всех драйверов (важно для работы сети).

2.  **Установите Vagrant** – инструмент для управления виртуальными средами.
    *   Скачайте с [официального сайта](https://www.vagrantup.com/).
    *   После установки **перезагрузите компьютер**.

3.  **Настройте WSL2 и Ansible (для Windows-хостов)**.
    *   Откройте PowerShell от имени администратора и выполните:
        ```powershell
        wsl --install -d Ubuntu-22.04
        ```
    *   После перезагрузки запустите дистрибутив Ubuntu из меню "Пуск" и завершите его первичную настройку (задайте имя пользователя и пароль).
    *   Внутри WSL Ubuntu установите Ansible и Git:
        ```bash
        sudo apt update && sudo apt upgrade -y
        sudo apt install ansible git -y
        ```

### 🖥️ Шаг 1: Загрузка проекта и подготовка образов VM
1.  **Клонируйте репозиторий проекта** в удобное место внутри WSL:
    ```bash
    git clone https://github.com/Verrat-dash/diploma_CIS_2025.git
    cd diploma_CIS_2025
    ```

2.  **Скачайте и подготовьте Vagrant box'ы**:
    В проекте используются пользовательские образы `ubuntu-gui` и `windows-gui`. Их необходимо добавить вручную **перед** запуском `vagrant up`.
    *   **Для Ubuntu:** Убедитесь, что у вас есть базовый образ Ubuntu с графическим интерфейсом.
    *   **Для Windows:** Вам потребуется самостоятельно подготовить Vagrant-совместимый образ Windows.

    > **💡 Совет:** Если у вас уже есть готовые `.box` файлы, добавьте их командой:
    > ```bash
    > vagrant box add ubuntu-gui /путь/к/вашему/образу-ubuntu.box
    > vagrant box add windows-gui /путь/к/вашему/образу-windows.box
    > ```

### 🔧 Шаг 2: Развертывание виртуальных машин
1.  **Запустите развертывание стенда.** Из папки проекта выполните:
    ```bash
    vagrant up
    ```
    *Этот процесс займет время (10-30 минут), так как Vagrant создаст две виртуальные машины:*
    *   **Ubuntu VM:** `ubuntu-cis-hardened`, IP: `192.168.56.10`, 3 ГБ RAM, графический интерфейс.
    *   **Windows VM:** `windows-cis-hardened`, IP: `192.168.56.20`, 4 ГБ RAM, графический интерфейс, настроен WinRM.

2.  **Проверьте статус машин:**
    ```bash
    vagrant status
    ```
    **Ожидаемый вывод:**
    ```
    Current machine states:
    ubuntu-cis-hardened          running (virtualbox)
    windows-cis-hardened         running (virtualbox)
    ```

### ⚙️ Шаг 3: Настройка пользователей для доступа Ansible
**Перед запуском Ansible необходимо настроить пользователей `vagrant` на обеих виртуальных машинах, так как они используются для подключения в `inventory.ini`.**

#### Для Ubuntu VM:
1.  Получите текущие учётные данные из Vagrant:
    ```bash
    vagrant ssh-config ubuntu-cis-hardened
    ```
    Обратите внимание на IP-адрес, пользователя и путь к приватному ключу.

2.  **Выполните команду настройки пользователя `vagrant` на Ubuntu VM** (замените IP на актуальный, если он отличается):
    ```bash
    ssh ubuntu@192.168.56.10 "sudo apt update && sudo apt install -y curl && sudo useradd -m -s /bin/bash vagrant && echo 'vagrant:vagrant' | sudo chpasswd && sudo usermod -aG sudo vagrant && sudo mkdir -p /home/vagrant/.ssh && sudo curl -o /home/vagrant/.ssh/authorized_keys https://raw.githubusercontent.com/hashicorp/vagrant/master/keys/vagrant.pub && sudo chown -R vagrant:vagrant /home/vagrant/.ssh && sudo chmod 700 /home/vagrant/.ssh && sudo chmod 600 /home/vagrant/.ssh/authorized_keys && echo 'vagrant ALL=(ALL) NOPASSWD:ALL' | sudo tee /etc/sudoers.d/vagrant && sudo sed -i 's/^#\?PasswordAuthentication.*/PasswordAuthentication no/' /etc/ssh/sshd_config && sudo sed -i 's/^#\?PubkeyAuthentication.*/PubkeyAuthentication yes/' /etc/ssh/sshd_config && sudo systemctl restart ssh"
    ```

#### Для Windows VM:
1.  Войдите в виртуальную машину Windows через графический интерфейс VirtualBox.
2.  Создайте локального пользователя с именем `vagrant` и паролем `vagrant`.
3.  Добавьте пользователя `vagrant` в группу **Администраторы**.
4.  Убедитесь, что служба WinRM запущена (это уже настроено в `Vagrantfile`).


### 🔐 Шаг 4: Запуск автоматизации безопасности с помощью Ansible
**Важно:** Убедитесь, что SSH-ключ для доступа к Ubuntu VM (`~/ubuntu_private_key`) существует. Если его нет, создайте его или скопируйте из Vagrant:

```bash
cp .vagrant/machines/ubuntu-cis-hardened/virtualbox/private_key ~/ubuntu_private_key
```
Протестируйте подключение к Ubuntu:

```bash
ansible -i inventory.ini ubuntu -m ping
```
Ожидаемый ответ: ```"ping": "pong"```

Примените настройки CIS для Ubuntu:

```bash
ansible-playbook -i inventory.ini playbooks/apply-ubuntu-cis.yml
```
Протестируйте подключение к Windows:

```bash
ansible -i inventory.ini windows -m win_ping
```
Ожидаемый ответ: "ping": "pong"

Примените настройки CIS для Windows:

```bash
ansible-playbook -i inventory.ini playbooks/apply-windows-cis.yml --ask-become-pass
```
При запросе пароля введите: vagrant (пароль по умолчанию для пользователя в Windows VM).

### ✅ Шаг 5: Проверка результатов и идемпотентность
Демонстрация идемпотентности:
Повторно запустите любой из плейбуков:

```bash
ansible-playbook -i inventory.ini playbooks/apply-ubuntu-cis.yml
```
При успешной настройке большинство задач будет в статусе OK или SKIPPED.

Ручная проверка на виртуальных машинах:

Для Ubuntu: Подключитесь по SSH:

```bash
vagrant ssh ubuntu-cis-hardened
sudo grep "minlen\|remember" /etc/security/pwquality.conf
exit
```
Для Windows: Войдите в ВМ через VirtualBox (логин: vagrant, пароль: vagrant). Откройте cmd:

```cmd
net accounts
```
В выводе должны отразиться применённые политики.

### 🛑 Шаг 6: Остановка и очистка стенда
Приостановить ВМ (сохранив состояние):

```bash
vagrant suspend
```
Полностью остановить ВМ:

```bash
vagrant halt
```
Удалить виртуальные машины (очистка диска):

```bash
vagrant destroy -f
```
Эта команда удалит созданные виртуальные машины, но не затронет ваши исходные файлы проекта.

##⚠️ Важные технические замечания
Настройка 5.2.4 (требование пароля для sudo)
В главном плейбуке `apply-ubuntu-cis.yml` присутствует **закомментированная** настройка:

```yaml
# - import_playbook: "roles/ubuntu-cis/tasks/5_Access_Control/5.2.4_Ensure_users_must_provide_password_for_privilege_escalation.yml"
#   tags: ['cis-ubuntu', 'section5', '5.2.4', 'all'] ```
```
Почему она закомментирована:
- Данная настройка требует ввода пароля для всех операций sudo
- После её применения все последующие Ansible-команды потребуют флаг ```--ask-become-pass```
- Это существенно замедляет процесс тестирования и отладки

Рекомендация по использованию:
1) Для первоначальной настройки и тестирования оставьте настройку закомментированной
2) В производственной среде раскомментируйте и выполните отдельно в последнюю очередь
3) После применения будьте готовы указывать --ask-become-pass для всех Ansible-команд
